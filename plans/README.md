# Improvement Plans — rtf-llm

Generated by the `/improve` advisor. Two runs:

- **Run 1** (2026-07-02, commit `e82a0f4`) — plans 001–012.
- **Run 2** (2026-07-20, commit `e82a0f4`) — plans 013–024, a re-audit that
  reconciled the existing index and surfaced 13 new findings. Plan 008 was
  found **DONE on disk** and reclassified; the "urllib3 bump" backlog item was
  found **RESOLVED** (uv.lock pins urllib3 2.6.3).

These plans are **self-contained handoffs for an executor** (a separate, possibly
less-capable model with none of the audit context). Each plan inlines its own
context, exact file paths, current-state excerpts, ordered steps with verification
commands, scope boundaries, and done criteria.

The advisor is **read-only on source code** — no source file was modified to
produce these plans.

## Selection

### Run 1 (2026-07-02)

The first audit surfaced 13 findings. The user selected three for planning: the
KeyboardInterrupt bug, the hardcoded `generate_report.py` duplication, and the
Python version / tooling misalignment. A gap analysis then identified four
high-priority attack families:
- TokenBreak / Tokenization Confusion (June 2025)
- Stored Prompt Injection via Document Upload (2026)
- Agent Memory Poisoning for Persistent Backdoors (OWASP AppSec USA 2025)
- Fallacy Failure / Logic-Based Jailbreaks (May 2025)

### Run 2 (2026-07-20)

The re-audit surfaced 13 new findings (plans 013–024 + this index reconciliation).
The user selected **all 13** for planning. Finding #13 (the stale index) is
delivered by this README update itself — no standalone plan.

Findings **not** yet planned (available on request from run 1): #4 auto-publish
of engagement output, #5 `BaseAttackModule` extraction, #6 unified module
registry, #9 sweep concurrency, #11 vendored `install.sh`, #13 god-file split.
The "urllib3 bump" item (#12) is now **RESOLVED** — removed from the backlog.
Direction items (turnkey `scan` subcommand, pluggable attack registry,
run-diff regression view) are options for the maintainer, not planned here.

### Run 3 (2026-07-20) — attack-vector focus re-audit

A focused re-audit of the **attack-vector surface** surfaced 15 new findings
(plans 025–039). The user selected all 15. Headline: the framework's "OWASP
LLM Top 10 coverage" is a marketing claim — 5 of 10 categories have zero
attack patterns, and `OWASP_MAPPING` uses non-canonical `LL` codes that match
neither OWASP v1.0 nor the current v1.1 (Nov 2024). Several attack categories
are bait-and-switch (GCG cited, no suffix; many-shot uses 7 examples; low-
contrast image is max-contrast). `technique_kb` has fabricated placeholder
arXiv/DOI citations. Plans 025–039 address the attack-surface correctness,
freshness, depth, and dead-content findings; direction work (new OWASP
categories, best-of-N/PAIR, membership inference) is noted as follow-up, not
planned here.

### Run 4 (2026-08-21) — full-audit re-run post engine build

A full re-audit at commit `a6a9a9d` (repo since moved into `red-teaming/`;
.venv recreated during the audit after relocation broke the pytest entry
point — see reconciliation below). The audit mapped the dual-stack reality
(legacy dispatch chain vs `modules/engine`, campaign runner as the only
engine-native consumer) and surfaced 14 findings. The user selected **all
14** for planning (plans 040–053).

Headlines: campaign reports attach results to the wrong technique under the
default shuffle (040); the public Pages site carries transcripts with
operator home paths (041); `engine.live` is unreachable via CLI so most of
the engine fires inert (042); canary ground truth is structurally absent
from campaigns (043); the scoring judge is untested with vote logic living
in an excluded script (044).


## Execution order & status

| Order | Plan | Finding | Effort | Depends on | Status |
|-------|------|---------|--------|-----------|--------|
| 1 | [001-test-baseline.md](001-test-baseline.md) | No verification baseline | M | — | **DONE** (reconciled run 4 — 21 test files, 153 passing; residual seams → 049) |
| 2 | [002-fix-summary-keyerror.md](002-fix-summary-keyerror.md) | `KeyError: 'attacks'` crash | S | — (test added by 001 if present) | TODO |
| 3 | [003-dashboard-localhost-bind.md](003-dashboard-localhost-bind.md) | Dashboard binds `0.0.0.0` | S | — | TODO (still open — confirmed) |
| 4 | [004-wire-or-remove-config-flags.md](004-wire-or-remove-config-flags.md) | Dead config keys | S | — | TODO |
| 5 | [005-remove-dead-indirect-injection.md](005-remove-dead-indirect-injection.md) | Dead module | S | — | TODO |
| 6 | [006-keyboard-interrupt-retry.md](006-keyboard-interrupt-retry.md) | Bare `except Exception` swallows Ctrl+C | S | — | TODO |
| 7 | [007-generate-report-consolidation.md](007-generate-report-consolidation.md) | Hardcoded `generate_report.py` duplication | S | — | TODO |
| 8 | [008-pyproject-alignment.md](008-pyproject-alignment.md) | Python 3.14 mismatch / no lint config | S | — | **DONE** (reconciled — see note) |
| 9 | [009-tokenbreak-attacks.md](009-tokenbreak-attacks.md) | TokenBreak / Tokenization Confusion attacks | S | — | **DONE** |
| 10 | [010-document-upload-injection.md](010-document-upload-injection.md) | Stored Prompt Injection via Document Upload | S | — | **DONE** |
| 11 | [011-agent-memory-poisoning.md](011-agent-memory-poisoning.md) | Agent Memory Poisoning backdoors | S | — | **DONE** |
| 12 | [012-logic-jailbreaks.md](012-logic-jailbreaks.md) | Fallacy Failure / Logic-Based Jailbreaks | S | — | **DONE** |
| 13 | [013-fix-judge-both-mode-failure-indicators.md](013-fix-judge-both-mode-failure-indicators.md) | Judge `both`-mode silent failure indicators | S | 001 (test only) | TODO |
| 14 | [014-remove-phantom-new-attacks-module.md](014-remove-phantom-new-attacks-module.md) | Phantom `new_attacks` module in reports | S | coordinate with 007 | TODO |
| 15 | [015-drop-unused-cryptography-dep.md](015-drop-unused-cryptography-dep.md) | Dead `cryptography` dependency | S | — | TODO |
| 16 | [016-intensity-gating-all-modules.md](016-intensity-gating-all-modules.md) | `--intensity` cosmetic in 8/10 modules | M | 001 (test only) | TODO |
| 17 | [017-save-config-atomic-write-key-stripping.md](017-save-config-atomic-write-key-stripping.md) | save_config unsafe write + incomplete key strip | S | — | TODO |
| 18 | [018-pytest-ci-gate-quality-workflow.md](018-pytest-ci-gate-quality-workflow.md) | No pytest CI gate | S | 001 | **DONE** (reconciled run 4 — quality.yml runs ruff/format/pytest) |
| 19 | [019-agents-md-executor-handoffs.md](019-agents-md-executor-handoffs.md) | No repo-level AGENTS.md | S | — | TODO |
| 20 | [020-fix-request-raw-retry-retry-after.md](020-fix-request-raw-retry-retry-after.md) | `_request_raw` over-retries 4xx + Retry-After parse | S | — | TODO |
| 21 | [021-ruff-mypy-pytest-config-sections.md](021-ruff-mypy-pytest-config-sections.md) | No `[tool.ruff]`/`[tool.mypy]`/`[tool.pytest]` config | S | — | **PARTIAL** (ruff+pytest sections exist; `[tool.mypy]` still missing) |
| 22 | [022-reuse-requests-session-llm-calls.md](022-reuse-requests-session-llm-calls.md) | No requests.Session reuse | S | land after 020 | TODO |
| 23 | [023-reconcile-usage-md-uv-rtf-cli.md](023-reconcile-usage-md-uv-rtf-cli.md) | USAGE.md contradicts README + tooling | S | — | TODO |
| 24 | [024-restrict-dashboard-to-report-file.md](024-restrict-dashboard-to-report-file.md) | Dashboard serves whole report_dir incl. log | S | complements 003 | TODO |
| 25 | [025-fix-owasp-taxonomy-ll-typo.md](025-fix-owasp-taxonomy-ll-typo.md) | OWASP taxonomy drift + `LL` typo | S | before new-OWASP-module work | TODO |
| 26 | [026-fix-low-contrast-image-inverted.md](026-fix-low-contrast-image-inverted.md) | low_contrast_hidden image is max-contrast | S | — | TODO |
| 27 | [027-many-shot-real-100-plus-shots.md](027-many-shot-real-100-plus-shots.md) | many-shot uses 7 examples (few-shot) | S | 016 (optional) | TODO |
| 28 | [028-fix-gcg-bait-and-switch.md](028-fix-gcg-bait-and-switch.md) | `adversarial` category is rephrasings, not GCG | S | — | TODO |
| 29 | [029-implement-weight-patch-backdoor.md](029-implement-weight-patch-backdoor.md) | patch/backdoor weight mod silently no-op | S | — | TODO |
| 30 | [030-fix-fabricated-arxiv-doi-citations.md](030-fix-fabricated-arxiv-doi-citations.md) | placeholder arXiv/DOI/CWE in technique_kb | S | — | TODO |
| 31 | [031-wire-defense-tester-canary.md](031-wire-defense-tester-canary.md) | defense_tester canary unwired from evaluator | S | — | TODO |
| 32 | [032-module-level-technique-kb-fallbacks.md](032-module-level-technique-kb-fallbacks.md) | 7 modules lack KB fallbacks; 3 key collisions | S | before new-module plans | TODO |
| 33 | [033-delete-or-wire-vendored-attack-py.md](033-delete-or-wire-vendored-attack-py.md) | vendored attack.py (52 templates) unwired | S/M | 034 (if wiring) | TODO |
| 34 | [034-retire-2023-dan-patterns.md](034-retire-2023-dan-patterns.md) | 2023-era DAN patterns stale | S | 033 (complementary) | TODO |
| 35 | [035-finish-role-confusion-stub-prompts.md](035-finish-role-confusion-stub-prompts.md) | 15 role_confusion stubs have no goal | S | — | TODO |
| 36 | [036-multimodal-vision-capability-detection.md](036-multimodal-vision-capability-detection.md) | vision check is provider allowlist, not probe | M | — | TODO |
| 37 | [037-rescope-weight-manipulation-extraction.md](037-rescope-weight-manipulation-extraction.md) | extraction is all asks; no logprob probing | L | 029 (separate half) | TODO |
| 38 | [038-adaptive-multi-turn-flows.md](038-adaptive-multi-turn-flows.md) | multi-turn flows are hardcoded, non-adaptive | L | — | TODO |
| 39 | [039-delete-or-repurpose-orphan-lab-modules.md](039-delete-or-repurpose-orphan-lab-modules.md) | 5 lab modules off-topic/stub, zero KB | S/M | — | TODO |
| 40 | [040-campaign-report-integrity.md](040-campaign-report-integrity.md) | Campaign shuffle misattributes techniques + last-seed data loss | S | land before 045 | TODO |
| 41 | [041-pages-exposure-triage.md](041-pages-exposure-triage.md) | Public Pages site publishes transcripts w/ operator paths | S/M | maintainer decision in Step 4 | TODO |
| 42 | [042-engine-live-cli-wiring.md](042-engine-live-cli-wiring.md) | `engine.live` unreachable via CLI; T10/T11 provider choices | S | land before 045/046/047 | TODO |
| 43 | [043-campaign-canary-ground-truth.md](043-campaign-canary-ground-truth.md) | Canary ground truth structurally dead in campaigns | M | 041 | TODO |
| 44 | [044-scoring-spine-llm-judge.md](044-scoring-spine-llm-judge.md) | Judge untested; vote-majority duplicated; veto scope wrong | S/M | before 052 | TODO |
| 45 | [045-checkpoint-resume-real.md](045-checkpoint-resume-real.md) | Resume resumes nothing; high-water never blocks overwrite | M | 040, 042 | TODO |
| 46 | [046-pipeline-error-isolation.md](046-pipeline-error-isolation.md) | One transform exception destroys the run; no partial report | S/M | coordinate main.py tail | TODO |
| 47 | [047-transform-construction-contract.md](047-transform-construction-contract.md) | TypeError-probing construction; `--pair-turns` ignored; rerank ties | S | coordinate with 040/042 | TODO |
| 48 | [048-architecture-decision-memo.md](048-architecture-decision-memo.md) | ADR: campaign-as-product vs freeze-the-engine | M | gates 050 wire choices + legacy-side plans | TODO |
| 49 | [049-engine-seam-test-baseline.md](049-engine-seam-test-baseline.md) | Engine seams untested; no root conftest (relocation-fragile) | M | before 050 | TODO |
| 50 | [050-dead-surface-sweep.md](050-dead-surface-sweep.md) | SWALLOW no-op; bypassed hardcoded False; orphaned backends | S/M | 049 first; 048 informs WIRE choices | TODO |
| 51 | [051-live-agent-blast-radius.md](051-live-agent-blast-radius.md) | `$HOME` workdir default; droid unsafe flag ungated | S | coordinate with 043 | TODO |
| 52 | [052-small-hardening-bundle.md](052-small-hardening-bundle.md) | mitm cleartext upstream; silent registry skips; rescore hardcode | S | 044 first | TODO |
| 53 | [053-packaging-portability.md](053-packaging-portability.md) | Console script cwd-bound; packages.find drift | M | 005 for wheel-content item | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Reconciliation notes (run 2)

- **Plan 008 → DONE**: fully implemented on disk since the 2026-07-02 audit.
  `pyproject.toml` has `requires-python = ">=3.10"`, the `[dependency-groups] dev`
  group (pytest/ruff/mypy), and `.github/workflows/quality.yml` matches 008's
  spec. `SUCCESSES.md` "Packaging Fix (2026-07-06)" confirms. The config-section
  portion ("no `[tool.ruff]` config") was NOT in 008's scope — it is now plan 021.
- **"urllib3 bump" → RESOLVED**: `uv.lock` pins urllib3 2.6.3 (Jan-2026 release).
  Removed from the unplanned backlog; no plan needed.
- **Plan 003 → still OPEN**: `main.py:65` `serve_dashboard(host='0.0.0.0', ...)`.
  Default still `0.0.0.0`. Not stale; still actionable. Plan 024 complements it
  (restricts *what* is served, independent of the bind).

### Run 4 (2026-08-21, commit `a6a9a9d`)

- **Plan 001 → DONE on disk**: `tests/` holds 21 files (153 tests passing);
  CI runs pytest. The remaining coverage gap (engine seams) is re-scoped as
  plan 049.
- **Plan 018 → DONE on disk**: `.github/workflows/quality.yml` runs
  `ruff check`, `ruff format --check`, and `pytest`.
- **Plan 021 → PARTIAL**: `[tool.ruff]` and `[tool.pytest.ini_options]`
  exist in `pyproject.toml`; `[tool.mypy]` still absent (ad-hoc mypy over
  `modules/engine` + `main.py`: ~295 errors, unconfigured). Remaining scope:
  the mypy section only.
- **Plan 003 → still OPEN** (re-confirmed): dashboard bind default
  unchanged.
- **Run-1 unplanned item "auto-publish engagement output" → now PLANNED as
  041**; remove from the backlog list above.
- **Correction to "committed docs/reports is by-design"**: the clearance
  covered sweep reports as intentional Pages source. Run 4 revised this:
  git-tracked `opencode_campaign/` transcripts contain absolute operator
  paths, reports embed `target_system_prompt`/`canary_token`, and the Pages
  site was verified PUBLIC (`gh api repos/caretak3r/rtf-llm/pages` →
  `public: true`). Finding planned as 041.
- **Environment fact**: the checkout moved into `red-teaming/`;
  `.venv/bin/pytest`'s shebang pointed at the old path so `uv run pytest`
  silently fell through to Homebrew Python 3.14 (21 bogus collection
  errors). Fixed during the audit by recreating `.venv`. Root conftest
  bootstrap planned as part of 049.
- **Provenance reality**: `rtf.db` runs/findings tables have 0 rows ever —
  CLI engine mode never completed a recorded run. Motivates 042/045; the
  scheduled regression-harness direction item remains unplanned.

## Dependency notes

**Run 1:**
- **Land 001 first** — it is the safety net. Plans 002–012 are independent of one
  another and each carries its own offline verification, so they can run in any
  order after 001.
- Plans 009–012 (attack vectors) run independently of plans 002–008 (code quality).

**Run 2:**
- **018 depends on 001**: the pytest CI gate has nothing to run until 001 creates
  `tests/`. Land 001 + 018 together (or 018 right after).
- **014 overlaps 007**: both touch `generate_report.py`. If 007 rewrites the file
  first, fold 014's phantom-`new_attacks` removal into 007. If 014 lands first,
  007's rewrite must not re-add `new_attacks`. Do not run both in parallel.
- **024 complements 003**: 003 narrows the bind (localhost); 024 narrows the served
  surface (only the report file). Independent fixes; land together for defense-in-depth.
- **022 lands after 020**: both modify `modules/llm_client.py`. Land sequentially
  (either order); don't edit the same lines concurrently. 020's `_parse_retry_after`
  helper is independent of 022's session swap.
- **021 and 018 are complementary**: 018 adds the pytest CI *invocation*; 021 adds
  the `[tool.pytest]` *config*. Both can land independently.
- **Tests for 013, 016, 017, 020, 024 depend on 001's harness** (`tests/` dir +
  pytest). Each plan's code fix can proceed without 001, but the regression test
  is deferred until 001 lands. Each plan states this in its STOP conditions.
- **015, 019, 023 are fully independent** (dep removal, new doc, doc edit) — any order.

**Run 3 (attack-vector plans 025–039):**
- **025 (OWASP taxonomy fix) BEFORE new-OWASP-category direction work** — future
  modules targeting LLM03/LLM05/LLM10 need canonical `LLM` codes in
  `OWASP_MAPPING`/`OWASP_WEIGHTS` to score correctly. Land 025 early.
- **032 (KB fallbacks + collisions) BEFORE new-module plans** — resolves the
  3 key collisions and adds 7 module-level fallbacks so new modules don't add
  more collision risk. Coordinate with 025 (both touch KB/mapping contracts).
- **027 (many-shot) optional 016** — parameterizes shot count by intensity,
  aligning with 016's `--intensity` contract; land after 016 if 016 is in flight.
- **033 (wire vendored templates) + 034 (retire DAN) together** — the 2026
  `assimilation_override` category (033) replaces the retired 2023 DAN slots
  (034), keeping modern jailbreak coverage up. If 033 chooses delete (option a),
  034 still stands alone.
- **029 (patch/backdoor) + 037 (rescope extraction) are separate halves** of
  `weight_manipulation` — land sequentially; don't edit the same file concurrently.
- **037 and 038 are L-effort, deferrable** — the highest-leverage run-3 plans are
  025/026/027/028/029/030/031/032 (S-effort correctness/depth fixes); land those
  first. 037 (real logprob extraction) and 038 (adaptive multi-turn) are larger
  and can wait without blocking the S-effort wins.
- **Tests for 026, 027, 028, 029, 031, 032, 036, 037, 038 depend on 001's
  harness** — each plan's code fix proceeds without 001, but the regression
  test is deferred until 001 lands. Pure-data plans (025, 030) use KB-content
  assertion tests, also gated on 001.
- **039 (delete lab modules) corollary**: after deleting c2/payload_loader,
  `pycryptodome` may become unused — a plan-015-style follow-up can drop it.
  Don't auto-bundle; the maintainer may want pycryptodome for a future crypto module.

**Run 4 (plans 040–053):**
- **P0 trio lands first, independently**: 040 (report integrity), 041
  (exposure triage), 042 (live wiring). No interdependencies.
- **main.py engine block is serialized**: 040 → 042 → 045 → 046 → 047 all
  rewrite adjacent regions of the same block. Never run two of them in
  parallel; each plan's excerpts assume the previous one landed.
- **044 before 052**: rescore imports `majority_vote` from its new home in
  `eval/llm_judge.py`.
- **049 before 050**: the deletion/wire sweep needs seam tests as its
  safety net. **048 before any WIRE choice in 050** and before further
  investment in legacy-side plans (016, 033–039).
- **043 and 051 share the `--workdir` flag**: land together or strictly
  sequentially; both touch the same parser block.
- **053's wheel-content item assumes 005 landed** (deletes the dead module
  currently shipped in the wheel); otherwise independent.
- **Suite baseline**: 153 passed at `a6a9a9d`. Every run-4 plan must keep
  the suite green; each carries its own verification commands.

## Findings considered and rejected (do not re-audit)

### Cleared as by-design / not-a-finding

- **Stored XSS in HTML dashboard** — cleared. Output is escaped via `_esc_html` /
  `_safe_json` (server) and `esc()` (client). Not a finding.
- **Hardcoded secrets** — cleared. `config.json` `api_key` values are the literal
  placeholder `"not-needed"`; `.env` is git-ignored.
  **Correction (run 2):** the prior note "save_config strips keys" was INCOMPLETE —
  `judge.api_key` was not stripped. Plan 017 fixes this (recursive stripping). The
  clearance stands for `llm.api_key` / `providers[*].api_key` only.
- **SSRF to the configured target** — by-design for this authorized-testing tool.
- **`eval`/`exec`/`pickle` on untrusted input** — none in runtime code (only
  illustrative comments inside string-template stubs in disabled lab modules).
- **Lab-module gating** — all six lab modules (c2/persistence/exfil/payload_loader/
  polymorphic/weight) are gated behind `enabled: false` and return early when
  disabled; `main.py` re-checks before dispatch. None run by default.
- **Multimodal path traversal** — none; images generated in-memory, document-upload
  uses synthetic OCR strings, no `open()`/`Path` from config/CLI/LLM output.
- **Dashboard path traversal** — none; stdlib `SimpleHTTPRequestHandler` with
  `directory=` constrains `..` to `report_dir`. (Over-disclosure *within*
  `report_dir` is plan 024 — a real, low-severity finding.)
- **Committed build artifacts** — none; `__pycache__`/`*.pyc`/`build/`/`egg-info`/
  `.ruff_cache`/`_site`/`.venv` all gitignored. Committed `docs/reports/` (37 files)
  is by-design (Pages source). **Revision (run 4):** that clearance covered
  sweep reports as intended Pages content; tracked `opencode_campaign/`
  transcripts with operator paths + embedded system-prompt/canary metadata
  are a real finding — plan 041 — and the Pages site is verified public.
- **Circular imports** — none; `modules/__init__.py` is comment-only;
  `llm_client.py` + `config_manager.py` import zero sibling modules.
- **Secret stdout logging** — none; `llm_client.py` prints only status codes, never
  headers/api_key/payload.

### Already-planned (do not re-report)

- Plans 001–012 (above) and the run-1 unplanned items (`BaseAttackModule`
  extraction, unified module registry, sweep concurrency, vendored
  `install.sh`, god-file split). Run 4 planned the previously-unplanned
  auto-publish item as 041.

### Rejected as not-worth-doing (run 2)

- **Dashboard double-call to `discover_loaded_model`** at startup — sub-second,
  startup-only; not worth a finding.
- **`serve_dashboard` port-bump recursion** (`main.py:91`) — bounded by available
  ports in practice; not a finding.
