[project]
name = "llm-red-team-framework"
version = "0.1.0"
description = "Adversarial LLM red teaming framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "colorama>=0.4.6",
    "cryptography>=41.0.0",
    "pycryptodome>=3.19.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
]
